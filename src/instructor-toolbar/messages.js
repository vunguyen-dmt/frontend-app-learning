import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  viewCourseIn: {
    id: 'instructorToolbar.text.viewIn',
    defaultMessage: 'View course in:',
    description: 'View course in Studio',
  },
});
export default messages;